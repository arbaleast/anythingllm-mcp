{
  "_comment": [
    "IMPORTANT: For security, avoid hardcoding API keys in this file.",
    "This example uses VS Code's 'input' variable type to securely prompt for the API key.",
    "Alternatively, use environment variable injection mechanisms or a .env file with uv run."
  ],
  "mcpServers": {
    "anythingllm": {
      "type": "stdio",
      "command": "uv",
      "args": [
        "run",
        "--directory",
        "/absolute/path/to/anythingllm-mcp",
        "anythingllm-mcp"
      ],
      "env": {
        "ANYTHINGLLM_API_KEY": "${input:anythingllm_api_key}",
        "ANYTHINGLLM_API_BASE_URL": "http://localhost:3001"
      }
    }
  }
}